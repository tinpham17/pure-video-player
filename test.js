describe("Video Player", () => {
  it("should display given ad on time", () => {
    // declare adSeconds = 4
    // declare adDuration = 60
    // render a page with an video div and an ad div, calling initVideoPlayer with adSeconds and adDuration params above
    // query video element, set video.currentTime to adSeconds - 1
    // expect ad element is null
    // query video element, set video.currentTime to adSeconds
    // expect ad element is not null
    // query video element, set video.currentTime to adSeconds + adDuration
    // expect ad element is not null
    // query video element, set video.currentTime to adSeconds + adDuration + 1
    // expect ad element is null
  })
})
